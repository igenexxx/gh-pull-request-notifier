name: Deployment
on:
  pull_request:
    types:
      - opened
    branches:
      - 'develop'
      - 'master'
      - 'main'
      - 'test/**'
  push:
    branches:
      - 'develop'
      - 'master'
      - 'main'
      - 'test/**'
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Install dependencies
        run: |
          cd .github/actions/submit-pr-notification
          npm install
      - name: Check context
        uses: ./.github/actions/submit-pr-notification